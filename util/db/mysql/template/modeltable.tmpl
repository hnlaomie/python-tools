<field name="度量" type="GROUP">
{% for column in table.columns %}
    <field name="{{column.name}}" tableName="{{table.name}}" columnName="{{column.name}}" type="MEASURE" dataType="{{column.type}}" formatType="PERCENT"/>
{% endfor %}
</field>